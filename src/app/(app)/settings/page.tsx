import AppLayout from "@/components/layout/AppLayout";
import PlaceholderContent from "@/components/PlaceholderContent";

export default function SettingsPage() {
    return (
        <AppLayout>
            <PlaceholderContent 
                title="Settings"
                description="Manage your account settings, payment preferences, and notifications here. This page is currently under construction."
            />
        </AppLayout>
    );
}
