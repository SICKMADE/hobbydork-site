// This file is a placeholder for a future authenticated layout.
// For now, the logic is in `src/components/layout/AppLayout.tsx`
// and `src/app/page.tsx` handles the auth guard.

export default function AppRoutesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
