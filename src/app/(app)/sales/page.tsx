import AppLayout from "@/components/layout/AppLayout";
import PlaceholderContent from "@/components/PlaceholderContent";

export default function SalesPage() {
    return (
        <AppLayout>
            <PlaceholderContent 
                title="My Sales"
                description="Track and manage all your sales from this page. This feature is currently being built."
            />
        </AppLayout>
    );
}
