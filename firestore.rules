rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMP: allow absolutely everything for everyone.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}